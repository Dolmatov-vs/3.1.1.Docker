version: '3.1'
services:
  postgres:
    image: postgres:12-alpine
    ports:
      - '8080:8080'
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=data
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=qwerty123